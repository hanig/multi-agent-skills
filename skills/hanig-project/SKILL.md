---
name: hanig-project
description: Start a swarm project on this machine, or adopt a half-finished repo into one. Surveys the host and the code first, interviews the human only about what inspection cannot answer, writes a plan whose units have declared outputs, files a Linear project and issues after ONE approval, then dispatches and tracks it. Use when asked to start a project, set up work on a server, plan a repo, turn a repo into tickets, or file issues from a plan.
---

# The front door

<!-- BEGIN GENERATED DECLARATIONS: declarations.json -->

## Canonical behavior declarations

This section is generated by `scripts/declaration_registry.py` from
`declarations.json`; edit the registry, then run `write-body`.

- **`placement.behavior-deciding`:** A statement that changes what a project operator or agent asks, surveys, writes, validates, files, dispatches, trusts, closes, publishes, or does when a capability is missing is behavior-deciding and belongs in this body.
- **`placement.reference-elaboration`:** References may elaborate a registered declaration or hold non-deciding measurements and examples; they must not be the only source of a behavior decision. (Elaboration: [reporting-evidence.md](references/reporting-evidence.md), [survey-interview.md](references/survey-interview.md), [tracker-sync.md](references/tracker-sync.md), [unit-contract.md](references/unit-contract.md).)
- **`placement.reference-dialect`:** References use strict dialect version 1: only closed column-zero fences and valid same-line comments -- which is what a `<!-- declaration: id -->` marker is -- can exempt text; tabs, blockquotes, indented code, OTHER raw HTML, nested lists, and ambiguous structure are refused.
- **`capability.host-policy`:** Follow the active host's discovered project instructions and approval policy; treat AGENTS.md, CLAUDE.md, and MEMORY.md as project context rather than claims that the host loaded any particular capability.
- **`capability.tracker`:** Use only the current session's real connector and operations; if none is available, preserve the reviewed draft or outbox intent, report the pending synchronization to the owner, and never invent a receipt, ticket reference, or confirmation. (Elaboration: [tracker-sync.md](references/tracker-sync.md).)
- **`capability.install-boundary`:** Installing this skill never provisions accounts, copies credentials, changes permission or model defaults, or grants a worker the coordinator's authority.
- **`paths.skill-directory`:** Use `HANIG_PROJECT_DIR` and declared `HANIG_SKILL_DEP_ROOTS` only to locate the loaded project skill and its required swarm sibling; an `--only` installation must include `hanig-swarm` in the same prefix, arbitrary agent stores must never be searched, and project, plan, state, input, and output paths remain relative to the project directory.
- **`workflow.order`:** Run survey, interview, plan, ticket drafting and approval, dispatch, tracker drain, and evidence report in that order; each step's output is the next step's input.
- **`survey.read-before-ask`:** Run `survey.py` and read its results before speaking; never ask for a fact the host, repository, or survey can answer. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`survey.incomplete-walk`:** `truncated` and `stuck` are not the same fact: treat counts from any state other than `complete` as FLOORS, and name `stuck_at` instead of planning as if the count were complete. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`survey.partition-state`:** Read `allow_accounts`, `deny_accounts`, partition QoS and `qos_grptres`, and `max_mem_per_cpu_mb` before sizing work; `unknown` is never `unrestricted`, so report an unknown state instead of silently planning around it. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`adoption.context`:** For a half-finished repository, read its README, CONTEXT.md, MEMORY.md, architecture decisions, recent history, and existing outputs before proposing remaining work. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`repository.destination`:** For a discovered repository and remote: Adopt it. Do not ask. State the adopted remote and branch in one line before moving on. For a repository without a remote ask once whether and where to push, because staying local is a first-class answer; for no repository ask once whether source must outlive the attempt and recommend nowhere. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`repository.source-data`:** A repository is justified only when something must outlive the attempt directory as SOURCE; compute artifacts are data, reach shared paths only through promotion with a named approver, and nothing in this workflow may commit a swarm output to a repo. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`repository.creation-approval`:** Creating a repository is outward-facing and requires the same explicit stop-and-ask gate as tracker filing unless the request contains the literal phrase `swarm autopilot`.
- **`interview.judgment-only`:** Use `grill-with-docs` when installed, otherwise ask one recommended-answer question at a time, only about judgment THAT inspection cannot settle. At least: done criteria, the scientific claim, discardable work, budget, protected destinations, retry exposure, and reporting cadence. The list is a floor, not an exhaustive set -- a value `swarm.py schema` requires and inspection cannot supply is a question too. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`interview.retry-boundary`:** Ask the most work the owner is willing to repeat after one interruption and use that answer to set the retry boundary; never infer it from the DAG or scheduler.
- **`interview.dispatch-complete`:** Stop when the plan can RUN, not when questions run out: use `swarm.py schema` to enumerate every dispatch value, and if a value is still a placeholder, that is a QUESTION, not a detail to settle later. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`interview.reporting-cadence`:** Ask how often the owner wants a report while the run lasts -- hourly, at each unit's close, only when something needs them, or at the end -- recommending hourly while units are live. Capture cadence and escalation separately: "only at the end" is a legitimate cadence and never a legitimate escalation policy, and a NEEDS_HUMAN unit, an exhausted budget, a blocked merge or a conflict a machine must not settle reaches them regardless. Record both in the plan as a top-level `reporting` block, never in a session's memory, because a session dies and its successor inherits the plan rather than the conversation. It sits outside `plan_digest`, so changing cadence mid-run needs no --accept-plan-change. Nothing enforces cadence; the orchestrator honours it, and enforcing it is ARC-691's clock rather than a sentence here. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`plan.inputs`:** Declare every path, glob, and configuration file a command reads; empty inputs, placeholders, and globs matching nothing are refused.
- **`plan.scheduler-route`:** Declare a surveyed partition and account per scheduled unit: validation refuses a nonexistent partition by name and known allow/deny mismatches, stays silent on unknown account data, and cannot check whether the account string exists, so report unknown account data and remember that a typo may pass validation and fail only at submission. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`plan.promotion`:** Anything that must reach a shared path needs `promote_to` and a named approver; unit outputs remain relative to the exclusive attempt root.
- **`code.configuration`:** Ask for and declare `provider` and an explicit `mode` on every code unit. The owner must choose unattended autonomy or deliberate default-permission stalling; confirm the selected provider's exact mode spelling from `paseo run --help` or the provider before writing the plan, record the choice, and pass it verbatim. Mode has no portable or exhaustive positive set; the coordinator does not validate the mode against an exhaustive provider set, so clean validation is no evidence the value is supported and an unsupported value may fail only at provider dispatch. Permission bypass must never be chosen silently, and `bypass` is Claude's spelling and Codex rejects it. (Elaboration: [unit-contract.md](references/unit-contract.md).)
- **`code.target-branch`:** Every code unit must name `repo` and the pull request destination as `target_branch`; the coordinator owns the source branch and legacy `branch` is not a fallback. (Elaboration: [unit-contract.md](references/unit-contract.md).)
- **`runtime.contract`:** Declare each `slurm` and `pipeline` unit's runtime resolution, entrypoint, probe, and verification; a canary must use the same runtime, verbatim probe, partition, and account and be a DAG ancestor, while `runtime: none` is an explicit claim.
- **`retry.contract`:** Any unit with `max_attempts` above 1 must declare `retry.max_lost` in a metric capped by plan-level `retry_limits`; size the unit from the owner's accepted repeat exposure.
- **`cluster.memory-flag`:** Supply `--mem` when the survey reports `mem_flag_required`; when no survey fact was recorded, the planner remains responsible for the correct cluster flag.
- **`cluster.account-allowance`:** After reading both allow and deny states, ask the owner whether CPU-only work may run in an exclusively allowed idle partition and recommend yes when the shared queue is capped; a named denial is not a question, so say the partition is closed to the account and move on, while an unknown allow or deny state must be reported instead of asked around or used for a recommendation. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`cluster.memory-charging`:** Apply `ceil(mem_mb / max_mem_per_cpu_mb)` when the limit is set, quote the resulting charged CPU count, recommend shrinking per-job memory until the charged count is one the account can hold, and ask whether that footprint fits the budget and queue. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`cluster.qos-scope`:** State that surveyed `qos_grptres` covers only partition QoS and that account or association QoS may impose an unseen `GrpTRES`; `unrestricted` is not a promise that the job will run. (Elaboration: [survey-interview.md](references/survey-interview.md).)
- **`findings.interview`:** Ask whether the project has findings to publish. Not every project has any, so do not require `findings.json` when the deliverable is an artifact or merged code change. (Elaboration: [reporting-evidence.md](references/reporting-evidence.md).)
- **`unit.retry-size`:** Define units by declared outputs and size them by accepted retry exposure rather than presentation stages or command shape.
- **`judgment.by-kind`:** Judge `slurm` by Slurm accounting plus declared outputs, `pipeline` by launcher exit plus declared outputs, and `code` by agent lifecycle plus outputs plus a produced commit, each over the coordinator-pinned pre-dispatch artifact basis; closure remains a separate by-kind decision.
- **`slurm.command-boundary`:** A `slurm` command is the work itself and must never invoke `sbatch` or `srun`; scheduler options belong in `sbatch` fields. (Elaboration: [unit-contract.md](references/unit-contract.md).)
- **`pipeline.command-boundary`:** A `pipeline` command is the workflow-engine invocation and requires fresh work and publish directories.
- **`code.prompt-boundary`:** The coordinator submits a code unit through `paseo run`; the unit uses `prompt`, never `command`, and provider, mode, model, thinking, and environment are fields rather than flags embedded in the prompt. (Elaboration: [unit-contract.md](references/unit-contract.md).)
- **`outputs.attempt-relative`:** Every unit output is relative to its attempt run directory; use `SWARM_UNIT_DIR` when a tool needs an absolute path and `promote_to` for shared publication. (Elaboration: [unit-contract.md](references/unit-contract.md).)
- **`code.default-agent`:** The default code agent is `codex/gpt-5.6-sol` with `thinking: high`; overrides belong on the unit, `thinking: null` suppresses the option, and mode remains explicit and provider-specific.
- **`slurm.array-outputs`:** A `slurm` unit must not combine `--array` with declared outputs; use one unit per shard or a separate merge unit over per-task paths. (Elaboration: [unit-contract.md](references/unit-contract.md).)
- **`plan.required-fields`:** Build `plan.json` from `swarm.py schema`: every unit declares `id`, `kind`, and `outputs`; `slurm` and `pipeline` declare `command`, while `code` declares `prompt`, plus dependencies, budget, write scopes, and surveyed scheduler flags as applicable.
- **`code.write-scopes`:** Treat `write_scopes` as planning metadata rather than process isolation; concurrently runnable code units need disjoint scopes or a dependency. (Elaboration: [unit-contract.md](references/unit-contract.md).)
- **`code.worktree-isolation`:** Rely on the coordinator's verified per-attempt Git worktree for code-path isolation, not `write_scopes`; worktrees isolate paths rather than Unix principals. (Elaboration: [unit-contract.md](references/unit-contract.md).)
- **`plan.docs-protection`:** Never overwrite an existing PLAN.md, MEMORY.md, or README.md when adopting a repository; write the swarm plan to `.swarm/PLAN.md` when the survey reports protected documents and tell the owner that the protected document caused the redirect.
- **`plan.human-document`:** The human PLAN.md must state what is being built, which interview decisions were made and by whom, and what is deliberately out of scope.
- **`plan.validate`:** Always run `swarm.py validate plan.json` before ticketing or dispatch and resolve every refusal rather than learning coupled fields one dispatch at a time.
- **`tracker.team`:** Default tracker work to team `Arc` without asking, and override it only when the owner explicitly directs another team; never use a Slurm account name as a tracker team.
- **`tracker.credential-boundary`:** Draft tracker work without network access on the shared login-node coordinator; tracker credentials and remote operations stay in the authorized connector session. (Elaboration: [tracker-sync.md](references/tracker-sync.md).)
- **`tracker.approval`:** Draft all tracker objects without network access, show the project and every issue title plus the count, and create nothing until one named explicit approval is recorded. (Elaboration: [tracker-sync.md](references/tracker-sync.md).)
- **`tracker.autopilot`:** Only the literal request phrase `swarm autopilot` bypasses the tracker approval stop for the whole run; ordinary assent does not count, and a recorded approval is not re-requested after rebuilding a draft.
- **`tracker.apply`:** After approval, create the project then issues, persist every returned id and identifier, and update objects keyed by unit id on later runs rather than duplicating them. (Elaboration: [tracker-sync.md](references/tracker-sync.md).)
- **`tracker.edges`:** The blockedBy interface is append-only without explicit removal: apply every `add_blocked_by` and `remove_blocked_by` relation, then read all edges back and provide them to the next draft. (Elaboration: [tracker-sync.md](references/tracker-sync.md).)
- **`tracker.readback-shape`:** A tracker edge read-back must include every issue, using an empty list for a known absence; without a read-back, removals remain `null`, and once filing occurred `check` treats that unknown state as drift. (Elaboration: [tracker-sync.md](references/tracker-sync.md).)
- **`tracker.attestation`:** Describe connector read-backs and outbox receipts as attested rather than verified, and never claim synchronization without naming the read time and source. (Elaboration: [tracker-sync.md](references/tracker-sync.md).)
- **`tracker.check`:** After apply and read-back, re-draft and run `tickets.py check` until both unit mapping and blocker edges agree.
- **`dispatch.sequence`:** Run `swarm.py run plan.json --dry-run` before live dispatch, then schedule `advance` with cron or a Paseo schedule; overlapping advances rely on the coordinator's OS lock.
- **`drain.authority`:** Let issue state follow coordinator unit state and never the reverse; tracker outages must not alter swarm state. (Elaboration: [tracker-sync.md](references/tracker-sync.md).)
- **`closure.evidence`:** Refuse any close intent without authorized closing evidence; report an issue closed without it as an integrity violation and reopen it rather than acknowledging the intent because the tracker looks done.
- **`closure.by-kind`:** Close `slurm` and `pipeline` issues only on predicate receipts; a code receipt authorizes only opening or linking a PR, and a code issue closes only on the matching merged pull request. Tracker acknowledgments and merge observations are attested, never verified.
- **`drain.block-intent`:** A `block` intent means an upstream unit will not complete; the applying session must name that upstream unit.
- **`outbox.receipt`:** Apply each pending intent through the connector before marking it applied, and record its outbox receipt only after tracker confirmation; an absent receipt means `unacknowledged`, never that the issue was not filed, and false acknowledgment is worse than missing acknowledgment. (Elaboration: [tracker-sync.md](references/tracker-sync.md).)
- **`outbox.idempotency`:** Use each intent's idempotency key so repeated drains are safe.
- **`report.required`:** A run is not finished until `report.py` produces an evidence report; publish its fragment and give the owner the artifact link. (Elaboration: [reporting-evidence.md](references/reporting-evidence.md).)
- **`report.evidence-source`:** Assemble reports only from plan, coordinator state, and receipts; never add a fact from narrative recollection when it is absent on disk. (Elaboration: [reporting-evidence.md](references/reporting-evidence.md).)
- **`report.contents`:** Report artifact digests, every receipt's stated limits, and project findings as unverified project claims; never drop a basis hedge while presenting DONE. (Elaboration: [reporting-evidence.md](references/reporting-evidence.md).)
- **`findings.contract`:** A unit that emits `findings.json` must declare it as an output and must also declare `promote_to` for the project directory so the report can read the promoted file. (Elaboration: [reporting-evidence.md](references/reporting-evidence.md).)
- **`findings.bound`:** Write actionable findings that state the measured bound, what the evidence does not prove, and deliberate omissions with the reason for each; never turn a sample with no observed defects into an unbounded no-defect claim. (Elaboration: [reporting-evidence.md](references/reporting-evidence.md).)
- **`adoption.remaining-work`:** On adoption, draft remaining units with outputs, check existing outputs before filing, and never turn a completed item or an artifact-free TODO into a unit. (Elaboration: [survey-interview.md](references/survey-interview.md).)

<!-- END GENERATED DECLARATIONS -->

## Host capability boundary

The generated capability declarations above are authoritative. Project files
describe context; they do not prove that this host loaded a tool, connector,
credential, account, or approval.

You are on a server. There is either an empty directory and an intention, or a
half-finished repo and a vague plan. The job is to get from there to units of
work that are dispatched, tracked, and verifiable, without asking the human
anything a machine could have looked up.

## Run from the loaded skills

Set `HANIG_PROJECT_DIR` to the directory containing this agent's actually
loaded `hanig-project/SKILL.md`. The resolver derives both this installed skill
and its declared `hanig-swarm` sibling from there, not from a Claude path or
the shell cwd. It reports a missing sibling before use; an `--only`
installation must include `hanig-swarm` in the same prefix. These variables
only locate programs: project-relative plan, state, input, and output paths
remain relative to the current project directory.

For a deliberate mixed or link installation, set
`HANIG_SKILL_DEP_ROOTS` to the known parent directory (or directories,
colon-separated) that contains the installed `hanig-swarm`; the resolver never
searches arbitrary agent stores. This is only needed when the two skills do
not share the loaded skill's parent.

```sh
export HANIG_PROJECT_DIR="/path/to/loaded/hanig-project"
P="$HANIG_PROJECT_DIR"
python3 "$P/scripts/skill_paths.py" self "$P" hanig-project >/dev/null || exit $?
S="$(python3 "$P/scripts/skill_paths.py" sibling "$P" hanig-project hanig-swarm)" || exit $?
```

Run in this order. Each step's output is the next step's input.

## 1. Survey. Never ask what you can read.

```sh
python3 "$P/scripts/survey.py" --repo . --out .swarm/survey.json
```

The survey reports host, Python, schedulers, partitions, accounts, memory-flag
requirements, disk, repository history and shape, protected documents, and
existing swarm state. Read it before speaking. A non-`complete` tree walk gives
floor counts, not totals; name a stuck directory rather than planning from an
incomplete count.

Read both allow and deny account states, partition QoS, `qos_grptres`, and
`max_mem_per_cpu_mb` before sizing work. `unknown` is not `unrestricted`.
For the measured failure modes and the memory-charge arithmetic, see
[survey and interview evidence](references/survey-interview.md).

For an adopted repository, also read its project documents, architecture
decisions, recent commits, and existing outputs before drafting remaining work.

## 2. Grill. One question at a time, each with a recommended answer.

Invoke `grill-with-docs` if it is installed; otherwise follow its rule
directly, which is the rule that matters:

> If a question can be answered by exploring the codebase, explore the
> codebase instead.

### Where code lives

Adopt a discovered repository with a remote and state the remote and branch in
one line. With no remote, ask once whether and where to push and recommend
staying local. With no repository, ask once whether source must outlive the
attempt and recommend nowhere. Compute artifacts are data, not repository
source; publish them only through promotion with a named approver.

Creating a repository is outward-facing. Stop for explicit approval unless the
request contains the literal phrase `swarm autopilot`.

### Finish only when the plan can dispatch

Ask only about judgment. At least: done criteria, scientific claim,
discardable work, budget, protected destinations, the most work the owner will
repeat after one interruption, and how often they want to be told what is
happening. That list is a floor, not an exhaustive set. Every question
includes a recommended answer.

Before ending the interview, enumerate the plan's required values:

```sh
python3 "$S/scripts/swarm.py" schema
```

Settle inputs, partition and account, shared-path promotion and approver, code
provider and mode, target branch, runtime and verification, and retry exposure.
Ask whether the project publishes findings. Supply `--mem` from surveyed facts,
and never treat a clean validation under unknown account data as an account
check. Stop only when every unit's reads and outputs can be stated without a
placeholder. Details and measured examples are in
[survey and interview evidence](references/survey-interview.md).

## 3. Plan. Units are defined by their OUTPUTS, not their commands.

Write two files. Size units by the retry answer from step 2. Any unit with
`max_attempts > 1` declares the corresponding retry exposure. The sibling
`hanig-swarm/SKILL.md` declarations `retry.boundary`, `retry.checkpoint`, and
`retry.exposure` are the executable contract.

### The unit contract, by kind. Read this before writing a plan.

| | `slurm` | `pipeline` | `code` |
|---|---|---|---|
| `command` is | the work itself | the engine invocation | **not used** |
| the prompt goes in | n/a | n/a | `prompt`, and only there |
| submitted by | the coordinator, so **never** `sbatch`/`srun` here | the coordinator | `paseo run` |
| configured by | `sbatch` flags | `command` | fields: `provider`, `mode`, `model`, `thinking`, `env` |
| `outputs` are | relative to the run-dir | relative to the run-dir | relative to the run-dir |
| judged by | Slurm accounting + declared outputs | launcher exit + declared outputs | agent lifecycle + outputs + a produced commit |
| closed by | a predicate receipt | a predicate receipt | a **merged PR** |
| also needs | surveyed memory/account flags | fresh work and publish dirs | `repo`, `target_branch`, explicit `mode` |

Outputs are relative to the attempt run directory. A Slurm command is the work,
not a nested scheduler submission. A code prompt contains instructions, while
provider configuration remains in fields. The default code agent is
`codex/gpt-5.6-sol` at `thinking: high`, but mode is explicit and
provider-specific. The owner chooses unattended autonomy or a deliberate
permission stall; confirm the selected provider's spelling before recording
and passing it verbatim. Read failure examples in [unit contract
details](references/unit-contract.md).

Read the field reference before writing units:

```sh
python3 "$S/scripts/swarm.py" schema
```

Never combine `--array` and declared outputs in one Slurm unit. Use one unit per
shard or a separate merge unit over per-task paths.

`plan.json` for the coordinator. Every unit needs `id`, `kind`, and `outputs`;
`slurm` and `pipeline` use `command`, while `code` uses `prompt`. Add `needs`
for dependencies, `gpu_hours` for the budget, `write_scopes`, and this
cluster's own surveyed `sbatch` flags.

`write_scopes` are planning metadata, not process isolation. Concurrent units
need disjoint scopes or a dependency; code-path isolation comes from verified
per-attempt Git worktrees. Every code unit names `repo` and `target_branch`.
The sibling declarations `code.write-scopes`, `code.worktree-identity`, and
the worktree limits state what that isolation does and does not establish.

`PLAN.md` for humans: what is being built, what was decided in step 2 and by
whom, and what is deliberately out of scope.

The survey reports `protected_docs`; preserve existing PLAN.md, MEMORY.md, and
README.md files, and put the swarm plan in `.swarm/PLAN.md` when needed.

Then, always:

```sh
python3 "$S/scripts/swarm.py" validate plan.json
```

It refuses a partition this cluster does not have, so a plan written for
another server fails here in one line instead of half-dispatching.

## 4. Tickets. Draft everything, create on ONE approval.

```sh
python3 "$P/scripts/tickets.py" draft plan.json --brief brief.json
```

This writes `tickets.json`. It talks to nothing: the coordinator runs on a
shared login node and a tracker token must not live there.

Default to team `Arc` without asking. Override only when directed with
`--team`; `charge_to` is a Slurm account, not a tracker team.

**THE DEFAULT IS TO STOP HERE.** The draft carries
`approval.state: "required"`, and while it says that, you may not create
anything. Filing a project is outward-facing: other people see it and undoing
it is manual.

The human clears it by saying so, and you record that:

```sh
python3 "$P/scripts/tickets.py" approve tickets.json --approver <name>
```

Only the literal request phrase `swarm autopilot` skips the gate for the whole
run. Pass `--autopilot` to `draft`; ordinary assent does not count. Do not
re-request an approval already recorded when rebuilding the draft.

Then, in this session, holding the Linear MCP connector:

1. Show the human the project and EVERY issue title in full, and the count.
   Not a summary. The titles are what they are approving.
2. Ask once. Not per issue.
3. On yes: create the project, then each issue, writing each returned id back
   into `tickets.json` (`project.linear_id`, `issues[].linear_id`,
   `issues[].identifier`).
4. Re-running later updates by unit id rather than duplicating.
5. Apply the DAG: for each issue, add every `add_blocked_by` relation, and
   **call `removeBlockedBy` for every `remove_blocked_by` entry.**
6. Read the edges back and hand them to the next draft, below. Applying
   without doing this leaves the next run unable to say whether the write
   landed.

### Reconcile the DAG from a read-back

`blockedBy` additions do not remove stale edges. After applying every addition
and `removeBlockedBy`, read every issue's relations back:

```sh
# In the session with the connector: list every issue in the project with its
# blockedBy relations, and write
#   {"schema_version": 1, "read_at": "<ISO 8601>", "source": "...",
#    "edges": {"<issue>": ["<blocker>", ...]}}
# Each key is the BLOCKED issue; handles may be unit ids, identifiers
# (ARC-236) or uuids. An issue with no blockers MUST appear with an empty
# list: omitting it is indistinguishable from not having looked.
python3 "$P/scripts/tickets.py" draft plan.json --tracker-edges edges.json
```

An issue observed without blockers appears with an empty list. Missing read-back
state leaves `remove_blocked_by` as `null`; after filing, `check` treats that as
drift. The connector's read-back is attested, not independently verified.
See [tracker synchronization details](references/tracker-sync.md).

Verify the two never drift:

```sh
python3 "$P/scripts/tickets.py" check plan.json tickets.json
```

It reports both halves: the unit/issue mapping, and the blockedBy edges as
read at a stated time. It never says "in sync" without naming that time.

## 5. Dispatch.

```sh
python3 "$S/scripts/swarm.py" run plan.json --dry-run   # shape first
python3 "$S/scripts/swarm.py" run plan.json
```

Then schedule `advance` (cron or a Paseo schedule). Overlapping runs are safe:
the coordinator takes an OS lock, so a second advance exits without acting.

## 6. Drain. Issue state follows unit state, never the reverse.

The coordinator records tracker intents as units change state:

```sh
python3 "$S/scripts/swarm.py" outbox --json
```

In a session with the connector, apply each pending intent to its issue, then
mark it applied:

- **Nothing closes on a self-report.** A `close` intent carries the unit's
  receipt. An intent without evidence must be REFUSED, not applied.
- **Two kinds of evidence, and they are not interchangeable.** Every intent
  names its `closing_evidence`. A `slurm` or `pipeline` unit closes on a
  predicate receipt. A `code` unit closes on a MERGED PULL REQUEST and never
  on a receipt: its receipt says an agent went idle and files exist, which is
  `open_pr`, not done. Apply an `open_pr` intent by opening or linking a PR
  and leaving the issue open.
- **An issue closed without its authorised evidence is an integrity
  violation.** Say so loudly and reopen it. Never mark the intent applied
  because the issue "looks done": that is the tracker being believed over the
  verdict, which is the whole failure this system exists to prevent.
- A `block` intent means an upstream unit will not complete. Say which one.
- **After the tracker confirms, record what landed**:
  `swarm.py outbox --state-dir DIR --record-receipt KEY --ref ARC-171`.
  Only after it confirms. A false acknowledgment is worse than a missing one,
  because re-draining is safe and un-filing is not. An intent with no receipt
  reads `unacknowledged`, which means this machine has no confirmation either
  way -- it does NOT mean the issue was never filed.
- Draining twice is safe: every intent has an idempotency key.
- A tracker outage must never alter swarm state. The swarm is authoritative;
  the tracker is a view of it.

Acknowledgment and synchronization details are in
[tracker synchronization details](references/tracker-sync.md).

## Last step, every run: the report

**A run is not finished until it has produced a report.** Not when the last
unit reaches DONE, and not when the issues close.

```sh
python3 "$P/scripts/report.py" . --out report.html            # standalone
python3 "$P/scripts/report.py" . --fragment --out frag.html   # to publish
python3 "$P/scripts/report.py" . --json                       # for a machine
```

Publish the fragment as an artifact and give the human the link. The report is
assembled only from `plan.json`, coordinator state, and receipts. It includes
artifact digests, receipt limits without strengthening their claims, and any
project findings labeled as unverified project claims.

When the interview establishes that findings exist, the producing unit declares
and promotes `findings.json`:

```json
{"outputs": ["findings.json"], "promote_to": "/abs/path/to/the/project"}
```

```json
{"findings": [{"title": "one sentence a reader can act on",
               "detail": "the numbers, and what they do NOT prove"}]}
```

State measured bounds, what they do not prove, and deliberate omissions. See
[reporting evidence details](references/reporting-evidence.md).

## Adopting a half-finished repo

Steps 1 and 2 change, the rest does not. Survey, read the docs, read the
history. Then bring the human a DRAFT of what you think remains, phrased as
units with outputs, and grill against that draft rather than from nothing. It
is far easier to correct a wrong list than to produce one from a blank page.

Do not file completed work or turn an artifact-free TODO into a unit. Preserve
the repository's existing documents and write `.swarm/PLAN.md` when the survey
reports a protected plan.

## What this never does

- Create or modify anything in a tracker without explicit approval in this
  session.
- Close an issue because an agent said so.
- Write to a shared canonical path. That is `swarm.py promote`, and it needs a
  named approver.
- Ask a question the survey already answered.
- Report a run as finished without a report, or write that report from
  recollection rather than from the receipts.
